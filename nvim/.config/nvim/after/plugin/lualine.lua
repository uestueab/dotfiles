require('lualine').setup {
    options = {
        --theme = 'auto',
        theme = "horizon"
    },

    -- sections = {
    --     lualine_a = {
    --         {
    --             'buffers'
    --             -- 
    --             -- show_filename_only = false,   -- Shows shortened relative path when set to false.
    --         }
    --     }
    -- }
}
